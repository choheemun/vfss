'''
test data 이외에 inference data가 따로 있어야 하며
inference data를 이미 tfrecord파일로 변환해두어야 한다
각 NN
즉 train_mask, train_pharyngeal, train_hyoid 모델을
모두 inference.py에 두고
각각의 check point 파일들을 불러 inference 시켜 결과를 얻으면 될듯

이후 시각화~~~~


'''